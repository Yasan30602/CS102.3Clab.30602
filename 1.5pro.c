#include<stdio.h>
int main()
{
    float n1,n2,avg;
    printf("Enter the first number ");
    scanf("%f",&n1);

    printf("Enter the second number ");
    scanf("%f",&n2);

    avg = (n1+n2)/2;

    printf("Average is %2f ",avg);
    retrun(0);
}
